import { Injectable } from '@nestjs/common';
// import * as puppeteer from 'puppeteer';

@Injectable()
export class RepuveService {
  /*async searchByPlate(plate: string) {
    //const browser = await puppeteer.launch();
    const page = await browser.newPage();
    await page.goto('https://www.repuve.gob.mx');

    // Aquí puedes utilizar puppeteer para automatizar la navegación en el sitio web del Repuve y extraer la información que necesitas
    // Por ejemplo, puedes utilizar los métodos page.type() y page.click() para llenar el formulario de búsqueda y hacer clic en el botón de búsqueda

    await browser.close();
    return { plate };
  }*/
}